spring.application.name=spring

# Database configuration - can be overridden by environment variables
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d3cf7c2dbo4c73e46p9g-a.oregon-postgres.render.com:5432/university_db_qhla}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:university_db_qhla_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:bR2SqcpuyvwFjjqOBunHL0jUhUvyzy3P}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}

# Mail configuration - can be overridden by environment variables
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:kwizeraolivier06@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:komc srlp bkjz nuns}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Server configuration
server.port=${SERVER_PORT:8080}

# Actuator configuration for health checks
management.endpoints.web.exposure.include=${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE:health,info}
management.endpoint.health.show-details=${MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS:when-authorized}

